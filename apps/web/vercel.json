{
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "rootDirectory": "apps/web",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/",
      "status": 200
    }
  ]
}
